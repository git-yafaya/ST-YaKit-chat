# DESIGN.md —— UI 与动效规范（唯一出处）

> 本文件是插件界面和动效的唯一依据，人工验收通过后冻结。规范变更走"提案→拍板"流程，不在实现过程中临时改。

## 1. 六套主题

### 1.1 颜色槽位（十三个，六套主题各自一份）

| 槽位 | 用途 |
|---|---|
| 面板底色 | 插件面板整体的背景 |
| 卡片底色 | 面板里再分出来的小块，颜色和背景稍微不同以示区别 |
| 分隔线 | 线条、边框的颜色 |
| 标题字色 | 标题用的字，比正文更重更黑 |
| 正文字色 | 主要阅读的文字 |
| 说明小字 | 次要的提示文字，比正文淡一点 |
| 主色 | 主要按钮、选中项、滑块已划过的轨道 |
| 主色上的文字 | 压在主色块上的字用什么颜色 |
| 点睛色 | 只用在"主操作按钮 / 关键状态指示灯 / 当前选中态（如开关打开状态）"，其余场合退回主色或中性色 |
| 点睛色上的文字 | 压在点睛色块上的字用什么颜色（各套单独定） |
| 成功 / 提醒 / 危险 | 三个状态色，六套主题共用同一组，只按底色深浅换一档 |

### 1.2 状态色（六套统一）

| | 成功 | 提醒 | 危险 |
|---|---|---|---|
| 深底用 | `#4CA97A` | `#E0A63C` | `#D4574B` |
| 浅底用 | `#2F8558` | `#A8731A` | `#B5433A` |

**点睛色与状态色的区分靠形状，不靠颜色**：
- 状态色只出现在状态条、徽章、提示框上，且必须同时带图标和文字
- 点睛色只出现在主操作按钮、关键状态指示灯、当前选中态（如开关打开）上，其余场合（比如滑块进度条）退回主色
- 两者不出现在同一种形状里

### 1.3 六套色值

**方案一【冷杉与海盐】** —— 浅底，极简理智。

| 槽位 | 色值 |
|---|---|
| 面板底色 | `#F8F9FA` |
| 卡片底色 | `#FFFFFF` |
| 分隔线 | `#E4E9EC` |
| 标题字色 | `#1A252C` |
| 正文字色 | `#495861` |
| 说明小字 | `#9AABB3` |
| 主色 | `#233D4D` |
| 主色上的文字 | `#FFFFFF` |
| 点睛色 | `#7F9EA1`（偏灰的海盐蓝） |
| 点睛色上的文字 | `#1A252C` |

质感：卡片投影 `0 12px 32px -4px rgba(35, 61, 77, 0.06)`。

---

**方案二【暮色无花果】** —— 浅底，温暖人文。

| 槽位 | 色值 |
|---|---|
| 面板底色 | `#FAF9F6` |
| 卡片底色 | `#FFFFFF` |
| 分隔线 | `#E8E1D6` |
| 标题字色 | `#2E1E17` |
| 正文字色 | `#6B5B52` |
| 说明小字 | `#A3958C` |
| 主色 | `#5E3023` |
| 主色上的文字 | `#FFFFFF` |
| 点睛色 | `#C08552`（焦糖琥珀） |
| 点睛色上的文字 | `#FAF8F5` |

质感：卡片投影 `0 12px 32px -4px rgba(94, 48, 35, 0.05)`。

---

**方案三【橄榄岩与日光柠檬】** —— 浅底，静谧先锋。

| 槽位 | 色值 |
|---|---|
| 面板底色 | `#F4F5F0` |
| 卡片底色 | `#FFFFFF` |
| 分隔线 | `#E2E5DC` |
| 标题字色 | `#141D18` |
| 正文字色 | `#4D5850` |
| 说明小字 | `#8C978E` |
| 主色 | `#1E2D24` |
| 主色上的文字 | `#FFFFFF` |
| 点睛色 | `#D8E874`（冷萃微柠，不许当文字颜色，只能当色块底色） |
| 点睛色上的文字 | `#141D18` |

质感：纯平面不用阴影，边框 `1px solid #E2E5DC`。

---

**中性深色**

| 面板底 | 卡片底 | 分隔线 | 标题 | 正文 | 小字 | 主色 | 主色上的字 | 点睛色 | 点睛色上的字 |
|---|---|---|---|---|---|---|---|---|---|
| `#17181A` | `#202226` | `#2F3237` | `#F2F3F5` | `#E7E8EA` | `#9B9EA4` | `#6B84A8` | `#FFFFFF` | `#8FA9CC` | `#17181A` |

**中性浅色**

| 面板底 | 卡片底 | 分隔线 | 标题 | 正文 | 小字 | 主色 | 主色上的字 | 点睛色 | 点睛色上的字 |
|---|---|---|---|---|---|---|---|---|---|
| `#F4F5F6` | `#FFFFFF` | `#DFE2E5` | `#14171A` | `#24262A` | `#6E737A` | `#46618A` | `#FFFFFF` | `#5B7FB0` | `#FFFFFF` |

### 1.4 跟随酒馆：槽位映射

| 槽位 | 取酒馆的哪个变量 |
|---|---|
| 面板底色 | `--SmartThemeBlurTintColor` |
| 卡片底色 | `--SmartThemeChatTintColor` |
| 分隔线 | `--SmartThemeBorderColor` |
| 标题字色 / 正文字色 | `--SmartThemeBodyColor`（酒馆没有专门标题色，靠字重字号拉层级） |
| 说明小字 | `--SmartThemeEmColor` |
| 主色 / 主色上的文字 / 点睛色 / 点睛色上的文字 / 成功 / 提醒 / 危险 | 回落，按宿主明暗取"中性深色"或"中性浅色"对应值 |

顺带跟随：卡片投影用 `--SmartThemeShadowColor`；消息条目底色用 `--SmartThemeBotMesBlurTintColor`。

**明暗判断**：看 `--SmartThemeBlurTintColor` 的亮度，不使用 `color-scheme`（酒馆写死 `only light`，据此判断必错）。
**不使用**：酒馆固定色常量（`--warning`/`--active`/`--fullred`/`--okGreen70a` 等，不在可配置主题槽位内）、已被注释废弃的 `--SmartThemeFastUIBGColor`。
**取不到值时**：该槽位走回落，不允许出现透明背景或白底白字。

### 1.5 跟随酒馆的质感修正（仅适用于"跟随酒馆"，不影响其余五套固定配色方案）

实测"跟随酒馆"模式出现"纯白面板 + 黑灰大阴影 + 高饱和蓝色"的廉价后台观感，修正如下：

- **面板底色 / 卡片底色**：宿主变量解析结果的亮度按 `0.2126R + 0.7152G + 0.0722B` 计算（0–255 区间），**≥250 视为"纯白或接近纯白"**，替换为暖白 `#FCFCFA`；低于此阈值时仍按 1.4 节正常取值，不额外处理
- **分隔线**：解析出的宿主边框色与同一主题下的面板底色按 **35% 边框色 + 65% 面板底色** 混合作为最终分隔线色，比宿主原始边框色更淡，降低对比度
- **弹窗投影**：不用四周均匀的浓重黑灰阴影，改为以向下投射为主的双层柔和阴影：`0 18px 46px rgba(20,24,32,.16), 0 3px 10px rgba(20,24,32,.07)`
- **遮罩层**（弹窗背后的页面遮罩）：不洗成灰白色，改用低透明度深色遮罩 `rgba(20,22,26,.18)`，可搭配极轻微的 `backdrop-filter: blur(2px)`
- **强调色（点睛色）应用方式**：分段控制器等靠整块背景表达"选中"的场景，选中项背景改用 **该主题点睛色 15% + 卡片底色 85%** 混合的浅色调，文字用该主题**点睛色原色**（不额外混合、不加深加浅）；这套比例对深色和浅色宿主（分别回落到"中性深色"/"中性浅色"点睛色）都适用，不用分别定义——背景本就是各自主题卡片底色与点睛色的混合，文字沿用各自点睛色原色，明暗会自动跟着所在主题走，天然可读。开关（Switch）这类必须靠色块本身表达开合状态、没有文字承载的场景不受影响，继续用实心点睛色；主要操作按钮（如"导出"）继续用实色主色填充，不受影响
- **关闭按钮**：去掉明显边框

整体目标：轻、柔、干净、克制的悬浮层质感，不出现黑边、黑雾、大面积纯白和厚重投影。

## 2. 布局

**弹窗背景 → 顶层抽屉 → 展开内容 → 二级抽屉**，不再有"内容底板"这一层专属的边框/投影容器——弹窗内部不套任何包住全部设置项的"大卡片"，避免"巨型卡片里嵌假抽屉"的结构。

- **弹窗尺寸固定，插件所有一级界面统一使用**：文本导出、设置、API 配置、提示词配置及后续新增一级界面共用同一套弹窗外壳尺寸，不得各自定义。PC 端 `width: min(960px, calc(100vw - 48px))`、`height: min(720px, calc(100vh - 48px))`，桌面空间足够时固定为 `960 × 720px`；窄屏按视口安全收缩，不产生横向溢出；弹窗居中。切换 Tab、展开/折叠抽屉、切换主题、切换导出格式、内容数量变化等任何交互都**不得改变弹窗外壳尺寸**——内容不足时保留空白，内容超出时只在内容区域内部滚动，弹窗本身的浮起投影见 1.3 各方案"质感"（原"卡片投影"，现为弹窗对宿主页面的投影）
- **弹窗纵向结构**：`标题区 → Tab 导航区 → 内容滚动区`。标题区与 Tab 导航区不参与正文滚动；内容区用 `flex: 1; min-height: 0; overflow-y: auto` 占满剩余高度并独立滚动。页面之间的差异只存在于内容区，不能因为内容多少改变弹窗外壳尺寸；面板底色就是内容的直接承载面
- **顶层抽屉纵向排列**：每个功能分组是一个独立抽屉，直接立在弹窗背景上，**不套任何有边框/投影的容器**；默认只显示单行 Header：标题居左、唯一箭头图标居右，整行可点击展开/折叠；默认折叠，多个抽屉可同时展开、不做"打开一个收起其他"的手风琴逻辑；箭头随展开状态旋转 180°、颜色跟随标题字色；展开/折叠动效用第 6 节"慢 `280ms`（抽屉面板展开收起）"
- **展开内容**：点击 Header 后内容从其下方自然展开；可用轻微缩进和留白区分层级，但**不再叠加背景色块或边框**；分组间距 `16px`，组内字段标签与控件间距 `8px`
- **二级抽屉（子抽屉）**：不使用完整外框，只靠标题整体缩进 `16px`、字重比顶层更轻（见第 3 节）、分隔线表现父子关系，不靠缩小字号；其余交互（单行 Header、默认折叠、互不排斥、280ms 动效）逐级复用顶层抽屉规则，不单独定义
- 抽屉之间、抽屉与子抽屉内容之间的分隔靠留白 + `1px` 分隔线，不引入新的背景色块
- **单个设置项**：标签在上、控件在下竖排，不用"左标签右控件"两栏表单
- 控件可点击区域至少 `32px` 高

## 3. 尺寸、圆角、字号

- **间距**：只用 `4 / 8 / 12 / 16 / 24`
- **圆角**：大容器大圆角、小控件小圆角——按钮/输入框/下拉选择器触发区 `6px`（不随弹窗放大同步放大）；弹窗 `20px`；开关/小标签全圆（`999px`）；抽屉（含子抽屉）本身无圆角、无独立背景；弹窗需正确设置 `overflow` 使内部滚动内容在四角处按弹窗圆角裁切，不能让滚动区域把圆角顶成直角
- **边框**：`1px`
- **字号**：大标题 `20px` / 小标题 `17px` / 正文 `16px` / 说明小字 `14px`
- **行高**：正文 `1.6`，标题 `1.3`
- **面板大标题**：`20px`、字重 `800`、行高 `1.3`
- **顶层抽屉标题**：`16px`、字重 `600`、说明小字色、行高 `1.6`（原"分组小节标题"/"卡片小节标题"）
- **子抽屉（二级）标题**：`14px`、字重 `400`（常规，比顶层更轻），配合 `16px` 左缩进区分层级
- **表单标签颜色**：正文色与说明小字色按 75%/25% 混合

## 4. 组件规格

| 组件 | 规格 |
|---|---|
| 开关（Switch） | 轨道 `40×24px`，圆点 `16×16px`，移动距离 `16px`，点击区高 `32px`；打开时轨道用点睛色，圆点用"点睛色上的文字"色 |
| 滑块（Slider） | 轨道高 `4px`，圆点 `16px`，操作区高 `32px`；已划轨道和圆点用主色，未激活轨道用分隔线色 |
| 下拉选择器 | 不用原生 `<option>` 的系统外观，浮层由插件自己渲染。触发区外观同文本输入框（边框分隔线色、圆角 `6px`，聚焦态同上一行规则），显示当前选中项文字和右侧展开箭头；浮层背景取卡片底色、圆角 `6px`、分隔线色 `1px` 边框、现有"卡片投影"，宽度跟随触发区，超出视口高度时内部滚动；选项行高 `≥32px`，未选中为正文字色，悬停/键盘高亮态背景为 10% 主色叠加 90% 卡片底色，当前选中项文字用主色且字重 `600`；展开/收起用动效表"中 `200ms`"档，出现用"出现/展开"曲线、收起用"消失/收起"曲线；键盘方向键移动高亮项、Enter 确认并收起、Escape 收起不选择、Tab 收起并移动焦点；点击浮层外部收起；触发区用 `role="combobox"`/`aria-haspopup="listbox"`/`aria-expanded`，浮层用 `role="listbox"`，选项用 `role="option"`/`aria-selected`，高亮项同步 `aria-activedescendant` |
| 文本输入框 / 文本域 | 边框沿用分隔线色，圆角 `6px`；聚焦时去掉浏览器默认 `outline`，边框颜色改为主色，粗细和圆角不变，不加阴影或外扩光晕 |
| 分段控制器 | 用于选项数量少且固定不变的单选场景（如主题选择、正则清洗模式、导出格式、提示词 target），**不用于数量会变化的动态列表**（如已保存的 API 配置/提示词记录，那些继续用列表+操作按钮或下拉选择器）。横向一排选项组成一个整体，圆角 `6px`、`1px` 分隔线外边框，选项之间以 `1px` 分隔线分隔；未选中项为正文字色、透明背景；选中项背景用点睛色、文字用"点睛色上的文字"色（与 Switch 打开态的选色逻辑一致）；点击直接切换，不展开浮层；切换过渡用动效表"快 `120ms`"档 |
| 滚动条 | 不使用系统/浏览器默认外观；宽度 `8px`，轨道透明；滑块用分隔线色、圆角全圆（`999px`），悬停时颜色加深至说明小字色；覆盖弹窗内部、下拉选择器浮层等一切可滚动区域；Webkit 用 `::-webkit-scrollbar` 系列自绘，Firefox 用 `scrollbar-width: thin` 搭配 `scrollbar-color` |
| 保存按钮 | 主色实心、纯白字，字重 `600`；顶部 92% 主色 + 8% 卡片色向下渐变到主色 |
| 按下反馈 | `scale(0.98)` |
| Tab（选项卡） | 面板顶部、大标题下方，用于面板本身的导航（如"设置/关于"切换）。文字标签横排左对齐，标签间距 `24px`，可点击区域高 `≥32px`；选中项标题字色+字重`600`、底部 `2px` 主色下划线；未选中项正文字色，无下划线；切换时下划线滑动过渡，用"出现/展开"曲线 `200ms`。每个Tab装的内容宁缺毋滥，内容多就拆分Tab，不堆在一个Tab里 |

## 5. 提示框 / Toast

背景：对应状态色（按深浅底取值）与卡片底色按 10% : 90% 混合；文字用状态色本身；左侧 `3px` 状态色竖条；内边距 `12px`；圆角 `6px`；必须同时带图标和文字。

三种类型不合并：
- **状态提示**：常驻在原地，人为触发收起
- **消息条目**：面板里的正文内容
- **Toast**：临时浮层，自动消失，不占布局空间——距顶部/右侧 `16px`，最大宽 `400px`；停留 `3000ms` 后开始退出，进出各 `200ms`、位移 `8px`

## 6. 动效

| 快慢 | 时长 | 用在哪 |
|---|---|---|
| 快 | `120ms` | 按钮按下、开关拨动 |
| 中 | `200ms` | 提示出现和消失、下拉展开 |
| 慢 | `280ms` | 抽屉面板展开收起 |

- 出现/展开曲线：`cubic-bezier(0.2, 0, 0, 1)`
- 消失/收起曲线：`cubic-bezier(0.4, 0, 1, 1)`
- 上限 `300ms`
- 系统开启"减少动态效果"时，所有动画降级为直接切换

## 7. 边界情况

- 跟随酒馆时宿主缺少某个颜色变量 → 回落到中性深色/中性浅色对应值，不允许透明背景或白底白字
- 预览区和页面本身的明暗互不影响
- 切换主题直接换色，不做过场动画
- 文字装不下换行或滚动，不许裁切

## 8. 版本

v1 —— 六套主题、状态色统一、点睛色使用边界、布局与组件规格、提示框与 Toast、动效曲线，均已人工验收通过。
v1.1 —— 新增 Tab（选项卡）组件规格，已人工验收通过。
v1.2 —— 新增文本输入框/文本域组件规格：聚焦态去除浏览器默认 outline，改用主色边框，待实现后人工验收。
v1.3 —— 布局改为"弹窗外壳→内容底板→功能分组"三级层级：新增内容底板承载全部分组，原功能分组去除各自的背景/圆角/投影改为透明区块，靠留白和分隔线组织；弹窗宽度由900px调整为`min(800px, calc(100vw - 48px))`。不新增颜色槽位，待实现后人工验收。
v1.4 —— "下拉框"改名"下拉选择器"并重写规格：不再使用原生 `<option>` 系统外观，浮层由插件自渲染，选项样式取主题色，保留键盘方向键/Enter/Escape/Tab 及 ARIA 可访问性。待实现后人工验收。
v1.5 —— 功能分组改为抽屉样式：标题行可点击展开/折叠，默认折叠，多个分组可同时展开、不互斥，复用已有的"抽屉面板展开收起"280ms 动效。待实现后人工验收。
v1.6 —— 新增滚动条组件规格：不用系统/浏览器默认外观，改为分隔线色细滚动条，悬停加深。待实现后人工验收。
v1.7 —— 抽屉支持嵌套：子抽屉标题行缩进16px，其余外观和交互逐级复用顶层抽屉规则。待实现后人工验收。
v1.8 —— **取消v1.3的"内容底板"专属容器**：不再有包住全部抽屉的大卡片/大边框背景，弹窗背景直接作为抽屉的承载面；顶层抽屉和子抽屉都不套边框/投影，只靠留白、缩进和分隔线区分层级；"卡片投影"（1.3节质感）改为弹窗自身对宿主页面的浮起投影。层级简化为"弹窗背景→顶层抽屉→展开内容→二级抽屉"。待实现后人工验收。
v1.9 —— 圆角层级整体提高：弹窗圆角由10px改为20px（大容器大圆角），按钮/输入框/下拉选择器等小控件圆角维持6px不变（小控件小圆角），弹窗需正确裁切内部滚动内容避免圆角顶成直角；明确子抽屉标题不缩小字号，改用字重400（比顶层600更轻）配合16px缩进区分层级。仍维持v1.8的扁平"弹窗→抽屉"结构，不恢复内容底板。待实现后人工验收。
v1.10 —— 新增"分段控制器"组件：选项数量少且固定的单选场景（主题选择、正则清洗模式、导出格式、提示词target）由下拉选择器改为分段控制器，一排选项点击直接切换、不展开浮层，选中态用点睛色；数量会变化的动态列表（已保存的API配置/提示词记录）不受影响，继续用原有形式。待实现后人工验收。
v1.11 —— 修正抽屉标题字号：**顶层改为16px**（原v1.9写的14px），字重600不变；二级维持14px/字重400/16px缩进不变。取代v1.9里"顶层14px"的表述。待实现后人工验收。
v1.12 —— 新增1.5节"跟随酒馆的质感修正"，仅针对"跟随酒馆"主题：面板/卡片底色避免解析出纯白（回落暖白#FCFCFA）、分隔线进一步降低对比度、弹窗投影改为双层柔和阴影、新增遮罩层规格（低透明度深色+轻微blur）、分段控制器选中态改为浅色背景+深色文字（Switch和主要操作按钮不受影响）、关闭按钮去掉明显边框。不影响其余五套固定配色方案。待实现后人工验收。
v1.13 —— **弹窗改为固定尺寸，插件全部一级界面统一使用**：PC端`width: min(960px, calc(100vw - 48px))`、`height: min(720px, calc(100vh - 48px))`，空间足够时固定960×720px，取代v1.3的800px宽度限定（不再只限宽度，同时固定高度）；切换Tab/展开折叠抽屉/切换主题/切换导出格式/内容数量变化均不得改变弹窗外壳尺寸，内容不足留空白、超出仅内容区内部滚动。新增弹窗纵向结构定义：标题区→Tab导航区→内容滚动区，标题区和Tab导航区不参与滚动，内容区用`flex:1; min-height:0; overflow-y:auto`占满剩余高度独立滚动。待实现后人工验收。
