# 纪实 -YaKit 设计规范

酒馆（SillyTavern）插件「纪实」的界面规范。写新组件、搭新页面前先读这份文档；和规范冲突的做法不要自由发挥，**先改规范，再改代码**。

目录：1 整体结构 · 2 标题层级 · 3 布局原则 · 4 颜色 · 5 字体与尺寸 · 6 动效 · 7 组件 · 8 文案 · 9 记住的设置 · 10 开发 · 11 待定事项

---

## 1. 整体结构

| 层 | 位置 | 说明 |
|---|---|---|
| 入口 | 酒馆输入框左边的魔法棒菜单 →「纪实」 | 界面总文件 `src/ui/panel/index.js` 的 `initPanelUI` 往 `#extensionsMenu` 里加一项 |
| 弹窗 | 屏幕居中，最大 880×720；手机（≤600px）铺满屏幕 | 原生 `<dialog>`，点暗处、按 Esc、点 × 关闭 |
| 顶部栏 | 左：「纪实」+ 同一行小字「-YaKit」；中：文字页签（导航在上方时）；右：主题插画按钮、关闭 | 三栏网格，页签真正居中；导航在上方且是手机时，页签换到第二行 |
| 底部导航 | 导航在下方时出现：一排只有图标的页签，不带文字 | 见 1.2 |
| 内容区 | 独立 iframe：`src/ui/page/index.html` | 酒馆的样式进不来，面板样式也跑不出去 |

### 1.1 页签

| 页签 | id | 底部图标 | 状态 |
|---|---|---|---|
| 文本导出 | `export` | `icons/ouput.svg` | 已搭好（见 3.1） |
| 润色 | `polish` | `icons/edit.svg` | 占位 |
| 预设 | `preset` | `icons/bookmark.svg` | 占位 |
| API 管理 | `api` | `icons/link.svg` | 占位 |
| 设置 | `settings` | `icons/settings.svg` | 已搭好（见 3.2） |

- 上方文字页签和下方图标页签是**同一组**，切换导航位置时保留当前页签。
- 切换页签时，面板里开着的抽屉自动收起。

### 1.2 导航栏位置

设置 → 导航栏位置：自动 / 上方 / 下方，默认「自动」。

- **上方**：顶部栏中间的文字页签（磨砂分段选择器）。
- **下方**：弹窗底部的图标页签，顶部栏只剩标题和右侧按钮。选中图标用主色、线宽 2px；没选中的用说明小字色、线宽 1.6px；图标 24px。
- **自动**：电脑用上方，其他设备用下方。
- **「电脑」的判断**（所有「自动」选项共用）：用鼠标（能悬停、指得准）**并且**窗口宽度 ≥ 768px；窗口大小或设备变化时重新判断。

### 1.3 顶部主题按钮

- 显示当前主题的插画（28px），点一下按清单顺序换到下一个主题。
- 换主题时插画跳一下（0.42s），按钮下方冒出主题名，1.4 秒后消失；「跟随ST」时名字带上美化名，如「跟随ST · 双色盒子-浅色毛玻璃」。

### 1.4 弹窗和面板怎么通信

两边只通过消息通信：

| 方向 | 消息 | 内容 |
|---|---|---|
| 弹窗 → 面板 | `dsh:tab` | 切到哪个页签 |
| 弹窗 → 面板 | `dsh:theme` | 当前主题；「跟随ST」时带上取到的颜色和字体 |
| 弹窗 → 面板 | `dsh:nav` | 导航栏位置设置，以及实际生效的是上方还是下方 |
| 面板 → 弹窗 | `dsh:set-theme` | 设置页里选了主题 |
| 面板 → 弹窗 | `dsh:set-nav` | 设置页里改了导航栏位置 |

面板和酒馆同源。**面板不直接写业务逻辑**，需要数据或执行操作时调用业务层接口 `parent.YaKitChat`（由 Codex 提供，接口说明写在对应页面脚本开头）。

---

## 2. 标题层级

| 层级 | 是什么 | 例子 | 旁边 / 下面放什么 |
|---|---|---|---|
| 一级 | 页签 | 文本导出、设置 | — |
| 二级 | 页面里的大块标题 | 卡片「导出预览」「正则匹配」；设置项「主题」「导航栏位置」；抽屉「导出设置」 | 行尾灰字写**当前选了什么**（如「冷杉与海盐」「自动 · 当前上方」） |
| 三级 | 大块里面的小标题 | 「切换方式」「显示位置」「楼层范围」 | 下面写**说明小字**（见 8.2） |

---

## 3. 布局原则

**主次分明：先想清楚这一页最主要是干什么的，把它放在最显眼的位置。**

1. **主要内容**：这一页的核心功能，放最上面、给足空间。
2. **紧密相关的操作**：直接影响主要内容的，放在主要内容下面，改了马上能看到效果。
3. **其他功能**：不常用或次要的，收进侧边抽屉（`<dsh-drawer>`），页面上只留入口。
4. **电脑上优先不用往下滚就能看完**（弹窗最大 880×720）；手机上不强求一屏，内容自然往下排。
5. **次级选项不能抢主次**：用小号控件（见 7.2）。

### 3.1 示例：文本导出（`src/ui/page/export.js`）

```
┌ 导出预览 ─────────────── 共 N 条 ┐  主要内容：占剩余高度，内容多了卡片内滚动
├ 正则匹配 ───── [删除匹配|只保留匹配] ┤  紧跟其下
├──────────────────────────────────┤
│ ⚙ 导出设置  当前设置摘要     [导出] │  底部操作条
└──────────────────────────────────┘
「导出设置」→ 右侧抽屉：楼层范围 / 消息类型 / 导出格式 / 文件名
```

- **导出预览**：副标题「最近 2 条消息导出后的样子」；匹配后为空显示「（匹配后为空）」；没有消息显示「（无可预览内容）」；业务接口还没接入时显示「文本导出还没接入」。酒馆里聊天变化时自动刷新。
- **正则匹配**：右上角小号分段选择器；顶部一行「识别到的标签」，列出最近两层楼原文里的标签按钮，点一下加入对应规则、再点取消（选中用点睛色底），没识别到时显示「最近两层没有识别到标签」；规则一行一条；写错的规则（由业务接口判断）下方提示「这条规则无效，已忽略」，其余照常生效。
- **底部操作条**：左边「导出设置」入口 + 当前设置摘要，右边点睛色「导出」主按钮。
- **导出设置抽屉**：楼层范围（全部楼层开关，关掉后填起止楼层）、消息类型（AI 回复 / 用户台词 / 系统提示）、导出格式（TXT / Markdown / EPUB；带类别标注 / 仅正文，下方小示例卡片随选择切换，展示两种写法的区别）、文件名（占位文字「不填就用角色名加日期」）。
- **特殊情况**：群聊显示「仅支持单人聊天」；没打开聊天显示「先在酒馆里打开一个聊天」；消息类型全关时导出按钮禁用，抽屉里出现危险色提示框；清洗后没内容时提示「没有可导出的内容」。

### 3.2 示例：设置页

- 二级设置项一行一个（`<dsh-collapse>`），点开**在原地往下展开**，同一时间只展开一项。
- 需要大面积展示的内容做成入口行（`link`，箭头朝右），点开宽抽屉。
- 当前设置项：

| 设置项 | 里面的三级选项 | 说明小字 |
|---|---|---|
| 主题 | 切换方式：自动 / 图标 / 下拉框；下面是主题选择区 | 选主题时显示插画还是下拉框。<br>建议选自动：电脑看插画，手机用下拉框。 |
| 导航栏位置 | 显示位置：自动 / 上方 / 下方 | 页签放在顶部还是底部。<br>建议选自动：电脑放上方，手机放下方。 |
| 组件示例 | 入口行，点开宽抽屉（760px），所有组件示例都在里面 | — |

- 主题「切换方式」只影响设置页里主题选择区的样子：图标 = 插画格子；下拉框 = 带插画的下拉选择；自动 = 电脑用图标，其他设备用下拉框。**顶部栏始终是插画按钮。**

---

## 4. 颜色

### 4.1 颜色槽位

组件里只写槽位名，**不写具体色值**（组件内部的兜底值除外），换主题时所有组件一起变。

| 槽位 | 变量 | 用途 |
|---|---|---|
| 面板底色 | `--panel-bg` | 弹窗内容区、面板页面背景 |
| 卡片底色 | `--card-bg` | 卡片、顶部栏、底部导航、输入框、抽屉、菜单 |
| 分隔线 | `--divider` | 分隔线、普通边框、悬停底色 |
| 标题字色 | `--text-title` | 标题、输入框里的文字、选中项文字 |
| 正文字色 | `--text-body` | 正文 |
| 说明小字 | `--text-muted` | 说明、占位灰字、图标默认色 |
| 主色 | `--primary` / `--on-primary` | 见 4.3 |
| 点睛色 | `--accent` / `--on-accent` | 见 4.3 |
| 状态色 | `--success` / `--warning` / `--danger` | 见 4.3 |
| 卡片质感 | `--card-shadow` / `--card-border` | 卡片投影或边框 |

主题只在带 `class="dsh-scope"` 的元素内部生效，**不能改动酒馆本身的样子**。

### 4.2 主题

主题清单 `src/ui/components/theme-list.js`，色值 `src/ui/components/themes.css`，插画 `src/ui/icons/theme/<主题名>.svg`。**用户选过的主题优先；没选过时默认「冷杉与海盐」。**

| 主题 | data-theme | 面板底 | 卡片底 | 分隔线 | 标题 | 正文 | 小字 | 主色 | 主色上的字 | 点睛色 | 点睛色上的字 | 质感 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 跟随ST | `tavern` | 见 4.4 | | | | | | | | | | |
| 浅色 | `light` | `#F4F5F6` | `#FFFFFF` | `#DFE2E5` | `#14171A` | `#24262A` | `#6E737A` | `#46618A` | `#FFFFFF` | `#5B7FB0` | `#FFFFFF` | 1px 分隔线边框 |
| 深色 | `dark` | `#17181A` | `#202226` | `#2F3237` | `#F2F3F5` | `#E7E8EA` | `#9B9EA4` | `#6B84A8` | `#FFFFFF` | `#8FA9CC` | `#17181A` | 1px 分隔线边框 |
| 冷杉与海盐 | `fir` | `#F8F9FA` | `#FFFFFF` | `#E4E9EC` | `#1A252C` | `#495861` | `#9AABB3` | `#233D4D` | `#FFFFFF` | `#7F9EA1` | `#1A252C` | 投影 `0 12px 32px -4px rgba(35,61,77,.06)` |
| 暮色无花果 | `fig` | `#FAF9F6` | `#FFFFFF` | `#E8E1D6` | `#2E1E17` | `#6B5B52` | `#A3958C` | `#5E3023` | `#FFFFFF` | `#C08552` | `#FAF8F5` | 投影 `0 12px 32px -4px rgba(94,48,35,.05)` |
| 橄榄岩与日光柠檬 | `olive` | `#F4F5F0` | `#FFFFFF` | `#E2E5DC` | `#141D18` | `#4D5850` | `#8C978E` | `#1E2D24` | `#FFFFFF` | `#D8E874` | `#141D18` | 纯平面，边框 `1px solid #E2E5DC` |
| 暖橙晨曦 | `orange` | `#FAF6F1` | `#FFFFFF` | `#EDE3D8` | `#2B1D14` | `#5E4B3E` | `#8C7869` | `#8A4B22` | `#FFFFFF` | `#E39A5B` | `#2B1D14` | 投影 `0 12px 32px -4px rgba(138,75,34,.06)` |

不写 `data-theme` 时跟随系统：系统浅色用「浅色」，系统深色用「深色」。

状态色：

| | 成功 | 提醒 | 危险 |
|---|---|---|---|
| 深底用 | `#4CA97A` | `#E0A63C` | `#D4574B` |
| 浅底用 | `#2F8558` | `#A8731A` | `#B5433A` |

### 4.3 用色规则（必须遵守）

**点睛色与状态色的区分靠形状，不靠颜色。**

- **状态色**只出现在状态条、徽章、提示框、提示消息上，且必须**同时带图标和文字**。
- **点睛色**只出现在：主操作按钮、关键状态指示灯、当前选中态（页签滑块、分段选择器滑块、开关打开、当前主题、下拉选项的勾选标记、已选中的标签按钮）。
- **主色**用在其余场合：焦点框、输入框聚焦边框、加载转圈、进度条、底部导航选中的线条图标。
- 点睛色和状态色**不出现在同一种形状里**。
- 点睛色**只当色块底色，不当文字或线条颜色**（有的主题点睛色很浅，画成字或细线会看不清）。
- 按钮**没有红色「危险」样式**；删除这类操作先弹提示框让用户确认。
- 表单**出错时外框不变色**，只在下方显示危险色提示（图标 + 文字）。
- 浅底主题用「浅底用」状态色，深底主题用「深底用」状态色。

### 4.4 跟随ST

`src/ui/panel/tavern-theme.js` 读用户当前的酒馆美化（美化 JSON 写到页面上的 `--SmartTheme*` 最终值，美化 CSS 改过的也算），拼成一套槽位颜色：

| 槽位 | 取自 |
|---|---|
| 面板底色 | UI 背景色（Blur Tint） |
| 卡片底色 | 聊天背景色（Chat Tint） |
| 分隔线 | 边框色（Border） |
| 标题、正文 | 主要文本（Body） |
| 说明小字 | 斜体文本（Em） |
| 主色 | 引用文本（Quote） |
| 点睛色 | 下划线文本（Underline） |
| 字体 | 酒馆里实际显示的字体；美化 CSS 里 `@import` 的字体文件面板也加载一份 |

- 主要文本是浅色就当深底主题处理，状态色用「深底用」。
- 半透明颜色先铺到底色上变成实色。
- 面板底和卡片底几乎一样时，面板底略微加深；分隔线看不出时自动加深。
- 正文对比度至少 4.5 倍，标题至少 7 倍，说明小字至少 3.5 倍，不够时自动拉开。
- **只在第一次使用和美化变了时读取**：用酒馆设置里的美化名、各项颜色和美化 CSS 算一个指纹（不需要重算页面样式，很快），读取结果连同指纹存进 `dsh-tavern-theme`；之后指纹没变就直接用存好的结果，指纹变了才重读并更新。酒馆里换美化或改颜色时面板自动跟着变。圆角、间距这类设置每套美化叫法不同，不跟随。

---

## 5. 字体与尺寸

| 用途 | 字号 | 字重 | 颜色 |
|---|---|---|---|
| 弹窗标题「纪实」、抽屉标题 | 16px | 600 | 标题 |
| 标题旁小字「-YaKit」 | 13px（比标题小 3px） | 400 | 说明小字 |
| 卡片标题 | 15px | 600 | 标题 |
| 设置项标题（二级） | 14px | 500 | 标题 |
| 正文、输入框文字 | 14px | 400 | 正文 / 标题 |
| 三级标题（设置项里） | 13px | 400 | 标题 |
| 按钮、页签、表单名字 | 13px | 500 | 标题 |
| 说明小字、提示、字数、卡片副标题 | 12px | 400 | 说明小字 |

- 字体：系统无衬线字体（`system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`）；「跟随ST」时用酒馆字体（`--dsh-font`）。
- 圆角：弹窗 12px；卡片 12px；抽屉贴边一侧 0、另一侧 16px；按钮、输入框、下拉 8px；菜单 10px；分段底托 10px、滑块 7px（小号 8px / 5px）。
- 控件高度：小 28px；中 32px（按钮）/ 36px（输入框、下拉、图标按钮）；大 40px。
- 顶部栏高 56px，抽屉头部高 56px，设置项行高 52px，底部导航每格 52px。
- 图标：线条图标 24×24、线宽 1.75；按钮里 14–18px；主题插画在顶部 28px、设置格子 72px、下拉选项 22px。

---

## 6. 动效

- 滑动类（页签、分段滑块、抽屉、折叠展开）：`cubic-bezier(0.3, 0.7, 0.2, 1)`，约 0.25–0.32s；抽屉关闭 0.22s。
- 换主题：弹窗整体交叉淡化 0.22s（浏览器支持页面过渡时），等面板换好颜色和字体再显示新样子；「跟随ST」的颜色和字体在弹窗打开后空闲时提前准备好。
- 弹窗打开 0.18s（淡入 + 上浮）；关闭 0.12s ease-in（淡出 + 下沉，暗色背景一起淡出），点 ×、点暗处、按 Esc 都走同一个关闭动效。
- 悬停、颜色变化：0.15–0.2s。
- 滑块只有**用户点选时**才滑动；字体或宽度变化（例如换主题）时直接就位，不能跳。
- 可点击的卡片悬停上浮 2px；按钮按下缩到 0.97；底部导航图标按下缩到 0.9；开关按住时圆点拉长。
- 提示消息从上方滑入 0.28s，2.5 秒后淡出。
- 系统开启「减少动态效果」时关掉动画。

---

## 7. 组件

组件都放在 `src/ui/components/`，写成独立的自定义标签（Shadow DOM），重复加载不报错。面板页面用普通 `<script>` 引入；弹窗用 `import` 引入。**每个组件文件开头都写着完整用法**，这里只列要点。

### 7.1 组件清单

| 组件 | 文件 | 要点 |
|---|---|---|
| 嵌入框 | `embed-frame.js` `<embed-frame>` | 独立 iframe；加载中 / 失败状态；`fill` 铺满容器 |
| 图标 | `icon.js` `<dsh-icon>` | 读 `src/ui/icons/` 里的 SVG，线条粗细统一，颜色跟随文字 |
| 分段选择器 / 页签 | `segmented.js` `<dsh-segmented>` | 轻底托 + 点睛色滑动指示器；`tabs` 当页签用，`frosted` 磨砂，`block` 撑满，`size="sm"` 小号；方向键切换 |
| 卡片 | `card.js` `<dsh-card>` | 标题、副标题、右上角 `extra`、底部 `footer`；`interactive` 可点击 |
| 折叠项 | `collapse.js` `<dsh-collapse>` | 设置页的二级设置项；标题、行尾灰字、左侧图标；点开往下展开；`link` 变成入口行，触发 `activate` |
| 抽屉 | `drawer.js` `<dsh-drawer>` | 右 / 左 / 底部滑出；关闭有动画；开着时后面不滚动 |
| 按钮 | `button.js` `<dsh-button>` | `primary`（点睛色，一块区域最多一个）/ 普通 / `ghost`；三种大小；图标、只有图标、加载中、禁用、撑满 |
| 输入框 | `input.js` `<dsh-input>` | 名字、说明、出错提示、图标、清空、字数、多行自动变高、只读、禁用、必填 `*` |
| 下拉选择 | `select.js` `<dsh-select>` | 外观和输入框一致；选项可带说明、图标、分组、禁用；`searchable` 搜索；当前项右边点睛色小圆勾；下方放不下往上开；Esc 只收起菜单 |
| 开关 | `switch.js` `<dsh-switch>` | 打开时轨道点睛色；名字 + 说明；小号；禁用 |
| 提示消息 | `toast.js` `DshToast.show(文字, 类型)` | 右上角弹出，2.5 秒消失；成功 / 提醒 / 危险；状态色只用在图标和左侧细条 |
| 细滚动条 | `scrollbar.js` `DshScrollbar` | 4px；平时隐藏，滚动时或鼠标移上去时出现；面板页面用 `install(document)`，带滚动区域的组件在内部接入；**不要装到酒馆页面上** |

### 7.2 用哪个、用多大

- **次级选项用小号分段选择器**（`size="sm"`）：和它的名字排在同一行，名字在左、选择器在右，不要撑满整行。
- **选项本身就是这一块的主体时用常规大小、撑满整行**（`block`）。例：导出设置抽屉里的导出格式，抽屉就是为这些选项打开的。
- 设置列表里「文字在左、开关在右」时用小号开关。
- 选项固定 2–3 个用分段选择器；选项多或以后会增加用下拉选择。
- 同一块区域最多一个 `primary` 按钮，其他用普通或 `ghost`。
- 不常用的功能用抽屉；设置项用折叠项在原地展开。

### 7.3 设置项的标准写法

小主认可的样子，以后的设置项都照这个排：

```
┌ [图标] 主题                         冷杉与海盐  ⌃ ┐  二级：行尾灰字写当前选择
│ 切换方式                       [自动|图标|下拉框] │  三级：名字在左，小号控件在右
│ 选主题时显示插画还是下拉框。                      │  说明小字：做什么
│ 建议选自动：电脑看插画，手机用下拉框。            │           建议怎么选
│ [ 具体内容 ]                                     │
└──────────────────────────────────────────────┘
```

### 7.4 通用要求

- 图标统一用 `src/ui/icons/` 里的线条图标；主题插画在 `src/ui/icons/theme/`。
- 所有可操作元素都要有键盘焦点框（主色 2px），鼠标点击时不显示。
- 表单类组件（输入框、下拉选择）共用同一套外观：8px 圆角、分隔线边框，聚焦时主色边框加 3px 主色淡光晕。
- 只有图标的按钮、没有名字的开关，必须写 `aria-label`。
- 手机宽度（≤600px）下检查一遍布局。

---

## 8. 文案

### 8.1 总原则

- 用大白话，不用专业术语。
- 短：一句说清楚，写不下就换行，不写长段落。
- 按钮写动作：「导出」「添加规则」「完成」。
- 占位灰字写不填会怎样：「不填就用聊天名」。
- 空状态写现状：「（无可预览内容）」「还没有规则，导出原文。」
- 出错写结果：「这条规则无效，已忽略」，不责怪用户。

### 8.2 三级标题下的说明小字

- 固定两句：**第一句说这是做什么的，第二句给建议**。
- 每句尽量不超过一行，写不下就换行（用 `<br>`），不要为了放小字改动控件排布。
- 例：「页签放在顶部还是底部。<br>建议选自动：电脑放上方，手机放下方。」

---

## 9. 记住的设置

都存在浏览器的 `localStorage` 里（面板和酒馆同源，共用一份）：

| 键 | 内容 | 默认 |
|---|---|---|
| `dsh-theme` | 主题 id | `fir`（冷杉与海盐） |
| `dsh-nav` | 导航栏位置 `auto` / `top` / `bottom` | `auto` |
| `dsh-tavern-theme` | 「跟随ST」读取结果和美化指纹 | 第一次使用时生成 |
| `dsh-theme-switch` | 设置页主题切换方式 `auto` / `icon` / `select` | `auto` |
| （业务层保存） | 文本导出的规则和导出设置，界面通过 `exportUI.loadSettings / saveSettings` 读写 | 全部楼层、三类消息、TXT 带标注、无规则 |

---

## 10. 开发

- 插件目录就在酒馆的第三方插件目录里，改完直接刷新酒馆网页查看；需要在酒馆扩展设置里启用本插件。
- 面板 iframe 的沙盒允许同源（`allow-same-origin`）：酒馆服务器不给完全隔离的页面读插件文件；样式隔离不受影响。
- 界面文件都在 `src/ui/`，按职责拆分：

| 位置 | 负责 |
|---|---|
| `src/ui/style.css` | 弹窗外壳样式（`manifest.json` 加载） |
| `src/ui/panel/index.js` | 界面总文件：魔法棒入口、弹窗外壳、页签、主题按钮 |
| `src/ui/panel/tavern-theme.js` | 跟随ST取色 |
| `src/ui/page/index.html` | iframe 面板页面：只放结构 |
| `src/ui/page/style.css` | 面板公共样式 |
| `src/ui/page/main.js` | 面板公共逻辑：细滚动条、接收弹窗消息并派发 `dsh-theme` / `dsh-nav` 事件、切页签 |
| `src/ui/page/<页面>.js` + `<页面>.css` | 各页自己的逻辑和样式，如 `export`、`settings`、`demo`（组件示例） |
| `src/ui/components/` | 组件 |
| `src/ui/icons/` | 图标和主题插画 |

- 新增页面：结构写进 `page/index.html` 的 `data-page` 区块，逻辑和样式各建一个同名文件，在 `index.html` 里引用。
- 界面和业务通过接口说明对接，不共同修改同一个文件。

---

## 11. 待定事项

| 事项 | 现状 |
|---|---|
| 部分主题小字偏淡 | 冷杉与海盐、暮色无花果、橄榄岩与日光柠檬的说明小字，以及暮色无花果点睛色按钮上的字，对比度低于 4.5 倍，暂按原色值 |
| 导出设置抽屉的说明小字 | 三级标题下暂不加，小主需要时再加 |
| 文本导出业务接入 | 界面已就绪，等 Codex 在 `YaKitChat.exportUI` 上提供接口（说明见 `src/ui/page/export.js` 开头） |
| 旧界面功能 | API 管理、提示词、EPUB 偏好、外观等设置暂无对应界面，等确定各页主要功能后再搭 |
| 润色 / 预设 / API 管理 | 占位卡片，等确定各页最主要的功能后再搭 |
